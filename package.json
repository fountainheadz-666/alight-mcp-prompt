{
  "name": "alightmcp",
  "version": "1.0.0",
  "description": "Small MCP (Model Context Protocol) helper that fetches health-plan data from a local HTTP endpoint and exposes MCP tools over stdio.",
  "main": "index.js",
  "type": "module",
  "bin": {
    "alightmcp": "build/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && chmod 755 build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  },
  "files": [
    "build"
  ]
}
